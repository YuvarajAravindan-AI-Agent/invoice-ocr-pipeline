fastapi==0.115.0
uvicorn[standard]==0.32.0
zcatalyst-sdk==1.1.0
# DeepSeekExtractionJudge uses the openai SDK against DeepSeek's
# OpenAI-compatible API (no dependency conflict with zcatalyst-sdk,
# unlike anthropic>=1.0 — see git history if that's ever revisited).
openai==2.28.0
