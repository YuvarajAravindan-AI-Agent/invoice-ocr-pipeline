fastapi==0.115.0
uvicorn[standard]==0.32.0
zcatalyst-sdk==1.1.0
psycopg[binary]==3.2.3
# Pinned below the current latest (1.3.0) on purpose: anthropic>=1.0
# requires typing-extensions>=4.14, which conflicts with
# zcatalyst-sdk==1.1.0's typing-extensions~=4.12.1 pin — pip's resolver
# can't satisfy both. 0.85.0 is the newest version compatible with it.
anthropic==0.85.0
