# Implement the common contract for alibaba here. Example shape:
#
# module "common" {
#   source = "../modules/common"
#   # (the common module only declares variables/outputs — this
#   # provider's resources go directly in this file, taking the same
#   # var.service_name / var.region / var.cpu / etc. inputs)
# }
#
# resource "..." "this" {
#   # map blueprint/platform.yaml capabilities to alibaba resources
# }
